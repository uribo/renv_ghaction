name: renv_schedule_update

on:
  schedule:
    - cron:  '13 5 * * *'

jobs:
  renv_schedule_update:
    name: renv r package update
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2
      - uses: r-lib/actions/setup-r@v1
      - uses: actions/cache@v1
        if: startsWith(runner.os, 'macOS')
        with:
          path: ~/Library/Application Support/renv
          key: ${{ runner.os }}-renv-${{ hashFiles('**/renv.lock') }}
          restore-keys: |
            ${{ runner.os }}-renv-
      - name: Package Update Check
        run: |-
          Rscript -e "renv::restore(confirm = FALSE); renv::update(prompt = FALSE); renv::snapshot(prompt = FALSE)"
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v3.4.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: Update dependent packages version
          title: '[UPDATE] R packages'
          body: >
            This PR is auto-generated by 
            [create-pull-request](https://github.com/peter-evans/create-pull-request).
